[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = [
  "PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 npx prisma generate",
  "npx nest build"
]

[start]
cmd = "node dist/main"
