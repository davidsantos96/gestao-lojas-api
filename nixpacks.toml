[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 npx prisma generate",
  "npm run build"
]

[start]
cmd = "node dist/main"
